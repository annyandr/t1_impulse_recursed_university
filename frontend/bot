<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Чат-бот</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f9;
        }
        .chat-container {
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            height: 100vh;
            max-width: 600px;
            margin: 0 auto;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }
        .header {
            background-color: #4caf50;
            color: white;
            text-align: center;
            padding: 20px;
            font-size: 1.5rem;
        }
        .chat-box {
            padding: 20px;
            flex-grow: 1;
            overflow-y: auto;
            border-bottom: 1px solid #ddd;
        }
        .chat-message {
            margin-bottom: 10px;
        }
        .chat-message.bot {
            color: #4caf50;
            font-weight: bold;
        }
        .chat-message.user {
            text-align: right;
            color: #333;
        }
        .response-options {
            display: flex;
            justify-content: space-around;
            padding: 10px 0;
        }
        .response-options button {
            padding: 10px 15px;
            background-color: #4caf50;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1rem;
        }
        .response-options button:hover {
            background-color: #45a049;
        }
        .input-section {
            display: flex;
            padding: 10px;
            border-top: 1px solid #ddd;
            gap: 10px;
        }
        .input-section input[type="text"] {
            flex-grow: 1;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }
        .input-section button {
            background-color: #4caf50;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .input-section button:hover {
            background-color: #45a049;
        }
        .input-section label {
            display: flex;
            align-items: center;
            cursor: pointer;
        }
        .input-section label input {
            display: none;
        }
        .input-section label span {
            padding: 10px;
            background-color: #e0e0e0;
            border-radius: 5px;
            cursor: pointer;
        }
        .input-section label span:hover {
            background-color: #d6d6d6;
        }
    </style>
</head>
<body>

<div class="chat-container">
    <div class="header">
        Чат-бот
    </div>
    <div class="chat-box" id="chatBox">
        <div class="chat-message bot">Добро пожаловать! Чем я могу помочь?</div>
        <div class="response-options">
            <button onclick="selectOption('Вариант 1')">Вариант 1</button>
            <button onclick="selectOption('Вариант 2')">Вариант 2</button>
            <button onclick="selectOption('Вариант 3')">Вариант 3</button>
        </div>
    </div>
    <div class="input-section">
        <label>
            <input type="file" id="fileInput">
            <span>📎</span>
        </label>
        <input type="text" id="messageInput" placeholder="Введите сообщение" onkeypress="checkEnter(event)">
        <button onclick="sendMessage()">Отправить</button>
    </div>
</div>

<script>
    function selectOption(option) {
        const chatBox = document.getElementById('chatBox');
        
        // Добавить сообщение о выборе
        const userMessage = document.createElement('div');
        userMessage.className = 'chat-message user';
        userMessage.textContent = `Вы выбрали: ${option}`;
        chatBox.appendChild(userMessage);

        // Ответ чат-бота
        const botMessage = document.createElement('div');
        botMessage.className = 'chat-message bot';
        botMessage.textContent = `Интересный выбор! Чем я еще могу помочь?`;
        chatBox.appendChild(botMessage);

        // Прокрутка вниз
        chatBox.scrollTop = chatBox.scrollHeight;
    }

    async function sendMessage() {
    const chatBox = document.getElementById('chatBox');
    const messageInput = document.getElementById('messageInput');
    const message = messageInput.value.trim();

    if (!message) {
        alert('Пожалуйста, введите сообщение.');
        return;
    }

    // Добавить сообщение пользователя в интерфейс
    const userMessage = document.createElement('div');
    userMessage.className = 'chat-message user';
    userMessage.textContent = message;
    chatBox.appendChild(userMessage);

    try {
        // Отправить сообщение на сервер
        const response = await fetch('/parse', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
            },
            body: JSON.stringify({ message }),
        });

        if (!response.ok) {
            throw new Error(`Ошибка сервера: ${response.status}`);
        }

        // Получить ответ от сервера
        const data = await response.json();

        // Добавить ответ от бота в интерфейс
        const botMessage = document.createElement('div');
        botMessage.className = 'chat-message bot';
        botMessage.textContent = data.reply || 'Ответ от сервера отсутствует.';
        chatBox.appendChild(botMessage);
    } catch (error) {
        console.error('Ошибка при отправке данных:', error);

        // Показать сообщение об ошибке в чате
        const botMessage = document.createElement('div');
        botMessage.className = 'chat-message bot';
        botMessage.textContent = 'Произошла ошибка. Попробуйте позже.';
        chatBox.appendChild(botMessage);
    }

    // Очистить поле ввода и прокрутить вниз
    messageInput.value = '';
    chatBox.scrollTop = chatBox.scrollHeight;
}


    function checkEnter(event) {
        if (event.key === 'Enter') {
            sendMessage();
        }
    }
</script>

</body>
</html>
